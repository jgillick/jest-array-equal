{
  "name": "jest-array-equal",
  "description": "A couple straightforward jest array equality matchers.",
  "version": "0.0.1",
  "author": {
    "name": "Jeremy Gillick"
  },
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build",
    "test": "jest",
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc"
  },
  "dependencies": {
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "ts-jest": "^27.1.3",
    "jest": "^27.5.1"
  }
}
